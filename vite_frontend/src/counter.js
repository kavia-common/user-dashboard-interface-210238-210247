/**
 * Deprecated demo module from Vite starter.
 * Intentionally left empty to avoid bundling unused demo code.
 * Keeping file to prevent import path breakages if referenced elsewhere.
 */
export {}
